export enum TaskStatus {
  NOT_ALLOWED = "NOT_ALLOWED",
  NOT_STARTED = "NOT_STARTED",
  ACTIVE = "ACTIVE",
  DONE = "DONE",
}

export enum TaskPriority {
  LOW = "LOW",
  MID = "MID",
  HIGH = "HIGH",
}
