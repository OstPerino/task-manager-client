export interface Board {
  id: number;
  name: string;
  description: string;
}

export interface IBoard {
  id: number;
  name: string;
  description: string;
  githubURL: string;
  projectId: number;
}